apiVersion: v1
kind: Service
metadata:
  name: pointless-service
spec:
  selector:
    app: pointless
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
    